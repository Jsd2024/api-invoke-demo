import { ChangeDetectionStrategy, Component, Input } from '@angular/core';
import { ChatMessage } from '../../shared/models/chat.model';

@Component({ selector: 'app-chat-message', standalone: true, template: `<div class="message" [class.message--user]="message.sender === 'user'"><div class="message-avatar">{{ message.sender === 'bot' ? '✦' : 'You' }}</div><div class="message-bubble">{{ message.text }}</div></div>`, styles: [`.message { display:flex; gap:8px; align-items:flex-end; margin:12px 0; animation:message-in .25s ease both; } .message--user { flex-direction:row-reverse; } .message-avatar { flex:none; display:grid; place-items:center; width:26px; height:26px; border-radius:9px; background:#e4edf9; color:#326bb3; font-size:9px; font-weight:800; } .message--user .message-avatar { background:#e8e5ff; color:#6458bd; } .message-bubble { max-width:77%; padding:10px 12px; border-radius:14px 14px 14px 4px; background:#f1f4f8; color:#344054; font-size:13px; line-height:1.45; } .message--user .message-bubble { border-radius:14px 14px 4px 14px; background:#326bb3; color:#fff; } @keyframes message-in { from { opacity:0; transform:translateY(4px); } to { opacity:1; transform:translateY(0); } }`], changeDetection: ChangeDetectionStrategy.OnPush })
export class ChatMessageComponent { @Input({ required: true }) message!: ChatMessage; }
